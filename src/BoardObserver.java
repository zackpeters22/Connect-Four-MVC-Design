/**
 * Zack Peters
 * BoardObserver.java
 * 
 * BoardObserver.java is an interface that only states the methods an observer must implement
 */
public interface BoardObserver {
	
	
	//void just update the observer
	void updateBoard(int a, int b);
}
